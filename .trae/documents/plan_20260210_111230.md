# Milan Repair Manager Project Initialization Plan

I will initialize the "Milan Repair Manager" project following your "Plug-and-Play" architecture and "Unified Design Language" blueprint.

## 1. Project Initialization & Tech Stack
- **Initialize Next.js 15**: Setup with App Router, TypeScript, Tailwind CSS, and `src/` directory.
- **Install Dependencies**:
  - **UI/Icons**: `lucide-react`, `sonner`, `clsx`, `tailwind-merge`, `class-variance-authority`, `radix-ui` primitives.
  - **Backend/Data**: `prisma`, `@prisma/client`, `next-auth@beta`.

## 2. Design System Setup (Milan Industrial)
- **Tailwind Configuration**:
  - Configure colors: Brand (`Indigo-600`), Surface (`Slate-50/White`), Text (`Slate-900/500`).
  - Configure fonts: `Inter` (Interface), `JetBrains Mono` (Data).
  - Define border radius standards.
- **Utility Setup**: Create `src/lib/utils.ts` for class merging (`cn` helper).

## 3. Directory Structure Implementation
I will create the specific folder structure to support the "Plug-and-Play" architecture:
```text
src/
├── modules/
│   ├── shared/ (Mandatory UI: PageShell, SectionCard)
│   ├── core/   (System components: Sidebar, UserMenu)
│   └── repairs/ (Example business module)
├── services/   (Data logic)
└── components/ui/ (Shadcn base components)
```

## 4. Core Components Implementation
- **Mandatory Shared Components**:
  - `PageShell.tsx`: Standardized page wrapper with title, description, and action slots.
  - `SectionCard.tsx`: Standardized white card container for content.
- **Layout Components**:
  - `Sidebar.tsx`: Navigation menu with "Milan Industrial" styling (Slate-950).
  - `Header.tsx`: Top navigation bar.

## 5. Page & Routing Setup
- **Routes**:
  - `(auth)/login`: Login page UI.
  - `(dashboard)/layout.tsx`: Applies the Sidebar + Header layout.
  - `(dashboard)/page.tsx`: Dashboard home using `PageShell`.

## 6. Database Schema (Prisma)
- **Initialize Prisma**: Setup `prisma/schema.prisma`.
- **Define Models**: Implement `User`, `Ticket`, `Part`, `TicketPart` relations as specified.
- **Client**: Create `src/lib/db.ts` for the global Prisma client instance.

## 7. Documentation
- Create `PROJECT_BLUEPRINT.md` containing the vision and specs you provided.
